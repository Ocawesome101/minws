# Lua-ws

A minimal HTTP-only webserver.  It is not a good idea to use this.  Does no security checks or path sanitization *at all*, which is in itself a massive security hole.

This was primarily an experiment to see if I could; there was no thought given as to whether I *should*.
